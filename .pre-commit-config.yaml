repos:
  - repo: local
    hooks:
      - id: verify-quiet-precommit
        name: quiet repository verification
        entry: >-
          env PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m ai_guardrails verify
          --profile precommit --base-ref HEAD --staged
        language: system
        pass_filenames: false
