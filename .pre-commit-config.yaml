repos:
  - repo: local
    hooks:
      - id: verify-quiet-precommit
        name: quiet repository verification
        entry: python scripts/verify_quiet.py --profile precommit --base-ref HEAD
        language: system
        pass_filenames: false
