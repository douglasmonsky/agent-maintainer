"""Compatibility shim for context budget helpers."""

from agent_context import budget

apply_line_limit = budget.apply_line_limit
bound_single_item_text = budget.bound_single_item_text
bound_text = budget.bound_text
count_lines = budget.count_lines
