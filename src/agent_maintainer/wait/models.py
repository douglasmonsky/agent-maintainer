"""Compatibility imports for reusable wait output primitives."""

from agent_waits import models as wait_models

TIMEOUT_EXIT_CODE = wait_models.TIMEOUT_EXIT_CODE
WaitRepairCapsule = wait_models.WaitRepairCapsule
render_wait_capsule = wait_models.render_wait_capsule
