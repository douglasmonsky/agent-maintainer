<!-- docsync:object docs.release_0_1_0b5.overview -->
# Agent Maintainer 0.1.0b5 Release Evidence

## Release State

- Version: `0.1.0b5`
- Commit: `d09c73db82d8690a5ae49466be09eca6085c6b84`
- Git tag: `v0.1.0b5`
- GitHub release: <https://github.com/douglasmonsky/agent-maintainer/releases/tag/v0.1.0b5>
- TestPyPI workflow: <https://github.com/douglasmonsky/agent-maintainer/actions/runs/28654042862>
- PyPI workflow: <https://github.com/douglasmonsky/agent-maintainer/actions/runs/28654251109>

## Local Release Gate

Release-gate commands passed before or immediately after publishing:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m docsync check
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest tests/docs/test_public_docs_prose.py tests/docs/test_first_touch_docs.py tests/release/test_release_state.py -q
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src AGENT_MAINTAINER_RUN_RELEASE_TESTS=1 .venv/bin/python -m pytest -m release tests/release -q
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m agent_maintainer verify --profile precommit
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m agent_maintainer doctor --strict
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m agent_maintainer verify --profile full
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m agent_maintainer verify --profile ci --base-ref origin/main --compare-branch origin/main
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m agent_maintainer verify --profile security
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m agent_maintainer verify --profile manual
```

Manual profile reported the expected optional skip:

```text
trivy [skipped-disabled]: disabled by default
```

## TestPyPI Artifacts

```text
agent_maintainer-0.1.0b5-py3-none-any.whl
sha256: 5e9c38aa905689d2f19e10ab26fa608ca22362a826f7c75a3a36a13ca52d78cf

agent_maintainer-0.1.0b5.tar.gz
sha256: bc91af4206388188c91e88ccbca8bfe90e1e31cafd14ef4323c7a9c5fcd734c2
```

Smoke tests passed from a clean virtual environment:

```bash
python -m pip install --index-url https://test.pypi.org/simple --extra-index-url https://pypi.org/simple "agent-maintainer[core]==0.1.0b5"
agent-maintainer --help
archguard --help
docsync --help
agent-maintainer init --track core --target <tmp-repo> --preset existing-app
```

## PyPI Artifacts

```text
agent_maintainer-0.1.0b5-py3-none-any.whl
sha256: 5e9c38aa905689d2f19e10ab26fa608ca22362a826f7c75a3a36a13ca52d78cf

agent_maintainer-0.1.0b5.tar.gz
sha256: bc91af4206388188c91e88ccbca8bfe90e1e31cafd14ef4323c7a9c5fcd734c2
```

Smoke tests passed from a clean virtual environment:

```bash
python -m pip install "agent-maintainer[core]==0.1.0b5"
agent-maintainer --help
archguard --help
docsync --help
python -c "import agent_maintainer, archguard, docsync"
agent-maintainer init --track core --target <tmp-repo> --preset existing-app
```

## GitHub Release Assets

The GitHub prerelease attached both distribution artifacts:

```text
agent_maintainer-0.1.0b5-py3-none-any.whl
agent_maintainer-0.1.0b5.tar.gz
```
