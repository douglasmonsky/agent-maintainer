# Architecture Decisions

This directory records intentional changes to architecture policy, module
boundaries, Tach configuration, and other repository structure contracts.

When `tach.toml`, `tach.domain.toml`, or another architecture policy file
changes, add or update a decision note here explaining why the boundary change
is intentional and why it is not architecture drift.
